# API Contract — School Management System

## Conventions

* Base URL: `/api/v1/`
* Auth: `Authorization: Bearer <access_token>` on all protected routes
* Response envelope:

  ```json
  { "success": true, "data": {}, "message": "Action completed." }{ "success": false, "data": null, "message": "Error description.", "errors": {} }
  ```
* Pagination (all list endpoints):

  ```json
  {  "success": true,  "data": {    "count": 120,    "next": "/api/v1/students/?page=3",    "previous": "/api/v1/students/?page=1",    "results": []  }}
  ```

  Default page size: 20. Max: 100. Query param: `?page=2&page_size=50`.
* Timestamps: ISO 8601 UTC (`2024-09-01T08:00:00Z`)
* All IDs: UUID strings

---

## Auth

### `POST /api/v1/auth/login/`

**Public** · Throttle: `5/min`

Login uses `school_id` + `password` + `role`. The user selects their role on
the landing page before reaching the login form. The selected role is sent
with the credentials and validated server-side.

```json
Request: {
  "school_id": "STD001",
  "password": "AdminSetPassword123",
  "role": "STUDENT"
}
Response (normal login): {
  "data": {
    "access": "<jwt>",
    "refresh": "<jwt>",
    "force_password_reset": false,
    "active_role": "STUDENT",
    "user": { "id": "uuid", "school_id": "STD001", "full_name": "", "roles": ["STUDENT"] }
  }
}
Response (first login — password reset required): {
  "data": {
    "access": "<jwt>",
    "refresh": "<jwt>",
    "force_password_reset": true,
    "active_role": "STUDENT",
    "user": { "id": "uuid", "school_id": "STD001", "full_name": "", "roles": ["STUDENT"] }
  }
}
```

**Role validation rules (enforced server-side):**

* `role` must be one of: `STUDENT`, `TEACHER`, `ADMIN`, `PRINCIPAL`, `IT_SUPPORT`, `SUPER_ADMIN`
* The submitted `role` must be present in the user's assigned `UserRole` records
* If role is not assigned to the user → HTTP 403:
  `"You are not registered as a STUDENT. Please select the correct role."`
* `active_role` is stored in the JWT claims so middleware knows which portal to enforce

**Multi-role users:** A user with both `TEACHER` and `ADMIN` roles can log in
under either role by selecting the appropriate one on the landing page. The
`active_role` in the JWT determines which portal they enter and which
permissions are active for that session.

### `POST /api/v1/auth/first-login-reset/`

**Auth required** (restricted token, `must_change_password=True` users only) · Throttle: `5/min`

```json
Request: {
  "new_password": "MyNewSecurePass1!",
  "confirm_password": "MyNewSecurePass1!"
}
Response: {
  "success": true,
  "data": {
    "access": "<new_full_jwt>",
    "refresh": "<new_refresh_jwt>"
  },
  "message": "Password updated. You are now logged in."
}
```

On success:

* Sets `must_change_password = False`
* Blacklists the restricted token
* Issues fresh, unrestricted JWT pair
* Logs `FIRST_LOGIN_RESET` to `AuditLog`

Password validation rules (enforced server-side):

* Minimum 8 characters
* At least one uppercase letter
* At least one digit
* Cannot match the admin-set temporary password

### `POST /api/v1/auth/refresh/`

**Public** · Throttle: `10/min`

```json
Request:  { "refresh": "<token>" }
Response: { "data": { "access": "<token>" } }
```

### `POST /api/v1/auth/logout/`

**Auth required** · Blacklists refresh token.

```json
Request:  { "refresh": "<token>" }
Response: { "success": true, "message": "Logged out." }
```

### `POST /api/v1/auth/change-password/`

**Auth required** · Only for users where `must_change_password = False` (normal logged-in users)

```json
Request:  { "old_password": "", "new_password": "", "confirm_password": "" }
Response: { "success": true, "message": "Password updated." }
```

---

## Users

### `GET /api/v1/users/`

**Admin, IT Support** · Filterable: `?role=TEACHER&is_active=true&search=STD`

```json
Response.data.results: [
  {
    "id": "uuid",
    "school_id": "TCH003",
    "full_name": "Ama Asante",
    "roles": ["TEACHER"],
    "is_active": true,
    "must_change_password": false
  }
]
```

Uses `select_related('userrole_set__role')`. Paginated.

### `POST /api/v1/users/`

**Admin (creates Teacher/Student/Principal), Super Admin (any role)**

```json
Request: {
  "first_name": "Ama",
  "last_name": "Asante",
  "phone": "+233244000000",
  "email": "ama.asante@school.gh",
  "password": "TempPass123!",
  "roles": ["TEACHER"]
}
Response.data: {
  "id": "uuid",
  "school_id": "TCH004",
  "full_name": "Ama Asante",
  "roles": ["TEACHER"],
  "must_change_password": true,
  "message": "User created. They must reset their password on first login."
}
```

`school_id` is auto-generated by the server based on the role. Admin provides the
initial password — it is stored hashed. `must_change_password` is always `true`
on creation. Email is optional but recommended for future password recovery.

### `GET /api/v1/users/{id}/`

**Admin, Principal, IT Support, or self**

### `PATCH /api/v1/users/{id}/`

**Admin or self (own profile only)**

```json
Request: { "first_name": "", "phone": "", "profile_photo": "<file>" }
```

### `DELETE /api/v1/users/{id}/` (soft-delete)

**Admin, Super Admin** · Sets `is_active=False`.

### `POST /api/v1/users/{id}/assign-role/`

**Super Admin only** (for IT_SUPPORT role); **Admin** for other roles

```json
Request:  { "role": "TEACHER" }
Response: { "success": true, "message": "Role assigned." }
```

Invalidates `user:{id}:roles` cache.

---

## Students

### `GET /api/v1/students/`

**Admin, Principal** · Filter: `?level=1&program=GA&status=ACTIVE&search=STD`

```json
Response.data.results: [
  {
    "id": "uuid",
    "school_id": "STD042",
    "full_name": "Kofi Mensah",
    "level": 1,
    "program": "General Arts",
    "class_section": "1A",
    "status": "ACTIVE",
    "must_change_password": false
  }
]
```

### `GET /api/v1/students/{id}/`

**Admin, Principal, or own (student)**

### `PATCH /api/v1/students/{id}/`

**Admin** · Updates `class_section`, `status`, etc.

### `POST /api/v1/students/{id}/assign-program/`

**Admin**

```json
Request:  { "program_id": "uuid" }
Response: { "success": true, "message": "Program assigned. Core courses enrolled." }
```

Triggers signal → auto-enroll in 7 core courses for current term.

### `POST /api/v1/students/{id}/enroll-electives/`

**Admin**

```json
Request:  { "course_ids": ["uuid1", "uuid2", "uuid3", "uuid4"] }
Response: { "success": true, "data": { "enrolled": 4 }, "message": "Enrolled in 4 electives." }
```

Validates exactly 4 courses, all belong to student's program. Returns 400 if not.

### `GET /api/v1/students/{id}/courses/`

**Admin, Teacher (own course students), Student (own)**
Returns enrolled courses with `select_related('course', 'term', 'level')`.

### `GET /api/v1/students/{id}/fees/`

**Admin, Student (own)**
Returns all `StudentFee` records for student.

---

## Academic Year & Terms

### `GET/POST /api/v1/academic-years/`

**Admin** (write) · **All roles** (read)

### `GET/PATCH /api/v1/academic-years/{id}/`

**Admin** (write)

### `GET/POST /api/v1/terms/`

**Admin** (write) · **All roles** (read)

### `POST /api/v1/terms/transition/`

**Admin only** · Throttle: `2/hour`

```json
Request:  { "term_id": "uuid" }
Response: {
  "success": true,
  "data": {
    "promoted": 45,
    "graduated": 12,
    "new_term": "Term 1 2025/2026"
  }
}
```

Business logic:

* If `term_number < 3`: increment all active students' current term.
* If `term_number == 3` and `level < 3`: reset term to 1, increment level.
* If `term_number == 3` and `level == 3`: set `status=GRADUATED`.
* Mark all `StudentFee` with `status != FULLY_PAID` for old term as `OVERDUE`.
* Generate new `StudentFee` records for new term (Celery task).
  All wrapped in a DB transaction.

---

## Courses

### `GET /api/v1/courses/`

**All auth** · Filter: `?type=CORE&program=uuid&search=`

```json
Response.data.results: [
  { "id": "uuid", "name": "Core Mathematics", "code": "CMATH", "type": "CORE" }
]
```

### `POST /api/v1/courses/`

**Admin**

### `GET/PATCH/DELETE /api/v1/courses/{id}/`

**Admin** (write) · **All** (read)

### `POST /api/v1/courses/{id}/assign-teacher/`

**Admin**

```json
Request:  { "teacher_id": "uuid", "term_id": "uuid", "level_id": "uuid" }
Response: { "success": true, "data": { "assignment_id": "uuid" } }
```

---

## Course Content (Teacher)

### `GET /api/v1/assignments/{assignment_id}/resources/`

**Teacher (own), Student (enrolled), Admin, Principal**
Uses `select_related('assignment__course')`.

### `POST /api/v1/assignments/{assignment_id}/resources/`

**Teacher (own assignment only)** · Throttle: `upload`

```json
Request (multipart): {
  "title": "Week 1 Notes",
  "resource_type": "PDF",
  "file": <binary>
}
```

File stored to S3. MIME validated server-side.

### `GET /api/v1/assignments/{assignment_id}/outline/`

**Teacher (own), Student (enrolled), Admin**

### `PUT /api/v1/assignments/{assignment_id}/outline/`

**Teacher (own)**

```json
Request: {
  "weekly_topics": [
    { "week_number": 1, "title": "Introduction", "description": "..." },
    { "week_number": 2, "title": "Chapter 2", "description": "..." }
  ]
}
```

---

## Quizzes

### `GET /api/v1/quizzes/`

**Teacher (own courses), Admin, Principal** · Filter: `?course=uuid&status=OPEN`

### `POST /api/v1/quizzes/`

**Teacher (own assignment)**

```json
Request: {
  "assignment_id": "uuid",
  "title": "Week 3 Quiz",
  "max_attempts": 2,
  "due_datetime": "2024-10-15T23:59:00Z",
  "total_marks": 20,
  "questions": [
    {
      "question_text": "What is 2+2?",
      "question_type": "MULTIPLE_CHOICE",
      "marks": 2,
      "order": 1,
      "choices": [
        { "text": "3", "is_correct": false },
        { "text": "4", "is_correct": true }
      ]
    }
  ]
}
```

### `PATCH /api/v1/quizzes/{id}/`

**Teacher (own, DRAFT status only)**

### `POST /api/v1/quizzes/{id}/publish/`

**Teacher (own)** · Sets `status=OPEN`.

### `GET /api/v1/quizzes/{id}/`

**Teacher (own), Student (enrolled + OPEN), Admin**

### `POST /api/v1/quizzes/{id}/attempts/`

**Student (enrolled)** · Starts a new attempt.

```json
Response.data: { "attempt_id": "uuid", "attempt_number": 1, "questions": [...] }
```

Validates: `quiz.status == OPEN`, `attempt_number <= max_attempts`, not past due date.

### `POST /api/v1/quiz-attempts/{attempt_id}/submit/`

**Student (own attempt)**

```json
Request: {
  "answers": [
    { "question_id": "uuid", "choice_ids": ["uuid"] },
    { "question_id": "uuid", "text_answer": "Paris" }
  ]
}
Response.data: { "score": 18, "total": 20, "submitted_at": "..." }
```

Validates attempt is `IN_PROGRESS`. Auto-grades MC, TF, MULTIPLE_ANSWER.

### `GET /api/v1/quizzes/{id}/submissions/`

**Teacher (own), Admin, Principal** — all submissions with scores.

---

## Assignments

### `GET/POST /api/v1/course-assignments/`

**Teacher (own)** (write) · **Student (enrolled), Admin, Principal** (read)

### `POST /api/v1/course-assignments/{id}/submit/`

**Student (enrolled)** · Throttle: `upload`

```json
Request (multipart): {
  "text_content": "My answer...",
  "file": <optional binary>
}
```

Validates `assignment.status == OPEN`.

### `PATCH /api/v1/assignment-submissions/{id}/grade/`

**Teacher (own course)**

```json
Request:  { "marks_obtained": 15, "feedback": "Good work." }
Response: { "success": true }
```

---

## Fees

### `GET /api/v1/fee-structures/`

**Admin, Principal** · Filter: `?level=1&program=uuid&term=uuid`

### `POST/PATCH/DELETE /api/v1/fee-structures/{id}/`

**Admin**

### `GET /api/v1/student-fees/`

**Admin, Principal** · Filter: `?status=OVERDUE&term=uuid&level=1`

### `GET /api/v1/student-fees/{id}/`

**Admin, Principal, or own student**

### `POST /api/v1/student-fees/{id}/payments/`

**Admin**

```json
Request: {
  "amount": 500.00,
  "payment_method": "MOBILE_MONEY",
  "reference": "MOMO-12345",
  "paid_at": "2024-09-10T10:00:00Z"
}
```

### `POST /api/v1/student-fees/send-reminder/`

**Admin**

```json
Request: { "student_ids": ["uuid1", "uuid2"], "message": "..." }
```

Enqueues Celery task.

---

## Schedules

### `GET/POST /api/v1/timetables/`

**Admin** (write) · **All** (read) · Filter: `?level=1&day=0&term=uuid`

### `GET/POST /api/v1/exam-schedules/`

**Admin** (write) · **All** (read)

### `GET/POST /api/v1/holidays/`

**Admin** (write) · **All** (read)

---

## Announcements

### `GET /api/v1/announcements/`

**All auth** — returns only announcements addressed to requesting user.
Filter: `?is_read=false`
Uses `prefetch_related('announcementrecipient_set')`.

### `POST /api/v1/announcements/`

**Admin, Principal, IT Support**

```json
Request: {
  "title": "Maintenance Notice",
  "body": "System will be down...",
  "recipient_type": "ALL",
  "program_id": null,
  "level_id": null
}
```

Sets `is_published=False`. Publish separately or auto-publish.

### `POST /api/v1/announcements/{id}/publish/`

**Creator only**
Triggers Celery task: fans out `AnnouncementRecipient` rows.

### `POST /api/v1/announcements/{id}/read/`

**Recipient** · Marks `AnnouncementRecipient.is_read=True`.

---

## Reports

### `GET /api/v1/reports/academic-performance/`

**Admin, Principal** · Filter: `?term=uuid&level=1&program=uuid&course=uuid`

```json
Response.data: {
  "total_students": 120,
  "avg_score": 72.4,
  "pass_rate": 0.84,
  "course_breakdown": [...]
}
```

### `GET /api/v1/reports/fee-collection/`

**Admin, Principal** · Filter: `?term=uuid&status=OVERDUE`

```json
Response.data: {
  "total_expected": 1800000,
  "total_collected": 1200000,
  "outstanding": 600000,
  "breakdown_by_status": {...}
}
```

### `POST /api/v1/reports/export/`

**Admin, Principal** · Throttle: `report`

```json
Request:  { "report_type": "ACADEMIC_PERFORMANCE", "format": "CSV", "filters": {} }
Response: { "data": { "task_id": "uuid" } }
```

Async. Poll `/api/v1/reports/export/{task_id}/` for status + download URL.

---

## IT Support

### `GET/POST /api/v1/support-tickets/`

**All roles** (create own) · **IT Support** (view all)

### `PATCH /api/v1/support-tickets/{id}/`

**IT Support** (assign, update status)

### `POST /api/v1/support/reset-password/`

**IT Support only**

```json
Request:  { "user_id": "uuid", "new_password": "ResetPass123!", "reason": "User forgot password" }
Response: {
  "data": {
    "school_id": "STD042",
    "must_change_password": true
  },
  "message": "Password reset. User must set a new password on next login."
}
```

Sets `must_change_password = True` so the user is forced through the first-login
reset flow on their next login. Logs to `PasswordResetRequest` and `AuditLog`.

---

## Teacher Evaluation

### `POST /api/v1/evaluations/`

**Student (enrolled in that course, one per term)**

```json
Request: {
  "teacher_id": "uuid",
  "course_id": "uuid",
  "term_id": "uuid",
  "rating": 4,
  "comment": "Great teacher."
}
```

### `GET /api/v1/evaluations/`

**Admin, Principal** — aggregate view. **Teacher** — own received evaluations.

---

## Error Codes

| HTTP | Meaning                                                            |
| ---- | ------------------------------------------------------------------ |
| 400  | Validation error —`errors`field has field-level detail          |
| 401  | Missing or expired JWT                                             |
| 403  | Authenticated but role not permitted                               |
| 404  | Object not found                                                   |
| 409  | Conflict (duplicate enrollment, already submitted, etc.)           |
| 422  | Business rule violation (e.g. attempt limit exceeded, quiz closed) |
| 429  | Rate limit exceeded                                                |
| 500  | Internal server error — do not expose stack trace in production   |
